# Everything Agent 领域术语

个人助理中的会话、回合、后台任务与执行轨迹使用以下统一名称。

## 会话与执行

**Session（会话）**：一段持续的聊天，包含按顺序发生的多个回合。

**Turn（回合）**：一次用户提交触发的完整处理过程，结束于回复、失败或取消；其中可以包含多次推理和工具调用。
_避免_：用 Run 指代聊天回合，或把一次模型调用称作完整回合。

**Iteration（迭代）**：回合内部的一轮模型推理及其后续动作。

**Background Task（后台任务）**：独立于聊天请求生命周期执行的记忆写入或整理任务；由回合派生时保留来源回合，同一任务的重试仍属于该任务。

**Trace（执行轨迹）**：一次回合、后台任务或独立操作的可观察事件集合；派生任务有独立轨迹，并与来源回合关联。

**Evaluation Run（评估运行）**：针对数据集的一次实验执行，可以包含多个用例及其各自的助理回合。
